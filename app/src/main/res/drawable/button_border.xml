<?xml version="1.0" encoding="utf-8"?>
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <item>
        <shape android:shape="rectangle">
            <solid android:color="#FFFFFF" /> <!-- Background color -->
            <corners android:radius="8dp" /> <!-- Corner radius -->
        </shape>
    </item>
    <item android:right="2dp" android:left="2dp" android:top="2dp" android:bottom="2dp">
        <shape android:shape="rectangle">
            <solid android:color="#030047" /> <!-- Inner background color -->
            <stroke android:width="2dp" android:color="#030047" /> <!-- Border color and width -->
            <corners android:radius="8dp" /> <!-- Corner radius matching the outer shape -->
        </shape>
    </item>
</layer-list>